[Unit]
Description=lock after resuming from suspend
Before=sleep.target suspend.target

[Service]
User=%I
Type=forking
Environment=DISPLAY=:0
ExecStart=/home/%I/.files/%H/bin/lock

[Install]
WantedBy=sleep.target suspend.target
