#!/bin/sh

if [ -z "$XDG_RUNTIME_DIR" ] ; then
  export XDG_RUNTIME_DIR="/run/user/$(id -u)/"
fi

TMPBG="$(mktemp --suffix=-lockscreen.png)"
grim "$TMPBG"
convert "$TMPBG" -scale 50% -blur 0x5 -scale 200% "$TMPBG"
swaylock --ignore-empty-password --no-unlock-indicator --image "$TMPBG"
